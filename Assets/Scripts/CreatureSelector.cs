using UnityEngine;

public class CreatureSelector : MonoBehaviour
{
    // This script is now redundant since NetworkVisualizer handles creature selection
    // You can either delete this script or keep it as a backup
    
    void Start()
    {
        // Log a message to indicate this script is no longer needed
        Debug.Log("CreatureSelector script is no longer needed. NetworkVisualizer now handles creature selection and stats display.");
    }
} 